import { generateMetadata } from '@/utils/getSeoData';

export const metadata = generateMetadata('/connectus/seo', 'contact-us');

const Page = () => {
  return <main></main>;
};

export default Page;
